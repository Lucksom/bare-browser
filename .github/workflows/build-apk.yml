name: 2. Build Bare Browser APK (Fast Base + CCache)

on:
  workflow_dispatch:

jobs:
  validate-patches:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Pre-Validate Patches Syntax
        run: |
          echo "VALIDATING BASE PATCH SYNTAX"
          rm -f $GITHUB_WORKSPACE/patches/*0100* || true
          FAILED=false
          for p in $(find $GITHUB_WORKSPACE/patches -type f -name "*.patch" | sort); do
            ERR=$(git apply --stat "$p" 2>&1 || true)
            if echo "$ERR" | grep -q "corrupt patch"; then
              echo "FAILED (CORRUPT): $(basename $p)"
              FAILED=true
            fi
          done
          if [ "$FAILED" = "true" ]; then
            exit 1
          fi
          echo "ALL BASE PATCHES VALID!"

  build:
    needs: validate-patches
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 1. SETUP DISK & 10GB SWAP
      - name: Setup System Resources
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/boost "$AGENT_TOOLSDIRECTORY"
          sudo docker image prune --all --force || true
          sudo apt-get clean
          sudo apt-get autoremove -y -qq
          sudo swapoff -a || true
          sudo rm -f /swapfile /mnt/swapfile || true
          sudo fallocate -l 10G /mnt/swapfile || sudo dd if=/dev/zero of=/mnt/swapfile bs=1M count=10240
          sudo chmod 600 /mnt/swapfile
          sudo mkswap /mnt/swapfile
          sudo swapon /mnt/swapfile

      # 2. INSTALL PREREQUISITES & CCACHE
      - name: Install Prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq git curl python3 build-essential openjdk-17-jdk ninja-build patch zstd ccache

      # 3. CONFIGURE CCACHE
      - name: Configure CCache
        run: |
          mkdir -p ~/.cache/ccache
          ccache --set-config=max_size=8.0G
          ccache --set-config=compression=true
          ccache --set-config=compression_level=1
          ccache -z
          ccache -s

      # 4. RESTORE / SAVE CCACHE (Saves even if build fails!)
      - name: CCache Cache Storage
        uses: actions/cache@v4
        with:
          path: /home/runner/.cache/ccache
          key: ccache-bare-${{ github.sha }}
          restore-keys: |
            ccache-bare-

      # 5. SETUP DEPOT TOOLS
      - name: Setup Depot Tools
        run: |
          git clone --depth 1 https://chromium.googlesource.com/chromium/tools/depot_tools.git /home/runner/depot_tools
          echo "/home/runner/depot_tools" >> $GITHUB_PATH
          mkdir -p ~/.config
          touch /home/runner/depot_tools/metrics.cfg
          echo "opt-out" > /home/runner/depot_tools/metrics.cfg
          echo "DEPOT_TOOLS_METRICS=0" >> $GITHUB_ENV
          export DEPOT_TOOLS_METRICS=0
          /home/runner/depot_tools/ensure_bootstrap || /home/runner/depot_tools/update_depot_tools

      # 6. DOWNLOAD & EXTRACT BASE
      - name: Download and Extract Base Archive
        run: |
          mkdir -p /home/runner/chromium
          cd /home/runner/chromium
          gh release download chromium-bare-base -p "chromium-bare-base.tar.zst.part_*" --repo ${{ github.repository }}
          cat chromium-bare-base.tar.zst.part_* | zstd -d -T0 | tar -xf -
          rm -f chromium-bare-base.tar.zst.part_*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 7. APPLY PATCHES
      - name: Apply Patches
        run: |
          cd /home/runner/chromium/src
          find . -name "tab_switcher_arrays.xml" -delete || true
          for p in $(find $GITHUB_WORKSPACE/patches -type f -name "*.patch" ! -name "*0100*" | sort); do
            git apply --3way --ignore-whitespace "$p" || git apply --recount --ignore-space-change "$p" || patch -p1 --forward < "$p" || true
          done

      # 8. INJECT KIWI TAB SWITCHER SETTINGS
      - name: Inject Kiwi Tab Switcher Settings
        run: |
          cd /home/runner/chromium/src
          python3 -c '
          import os, glob

          # 1. Inject Preference into Tabs XML
          for root, dirs, files in os.walk("chrome"):
              for f in files:
                  if f in ["tabs_settings_preferences.xml", "tab_management_preferences.xml", "tabs_preferences.xml"]:
                      p = os.path.join(root, f)
                      with open(p, "r", encoding="utf-8") as xf:
                          c = xf.read()
                      if "tab_switcher_type" not in c:
                          tag = "\n    <ListPreference android:key=\"tab_switcher_type\" android:title=\"Tab switcher\" android:summary=\"%s\" android:entries=\"@array/tab_switcher_entries\" android:entryValues=\"@array/tab_switcher_values\" android:defaultValue=\"1\" />\n"
                          c = c.replace("</PreferenceScreen>", tag + "</PreferenceScreen>")
                          with open(p, "w", encoding="utf-8") as xf:
                              xf.write(c)

          # 2. Inject Arrays into existing registered values/arrays.xml or strings.xml
          arrays_data = """
              <string-array name="tab_switcher_entries">
                  <item>Default</item>
                  <item>Original (vertical, same as old Chromium)</item>
                  <item>Horizontal (same as old Chromium)</item>
                  <item>Grid (supports tab group)</item>
                  <item>List</item>
              </string-array>
              <string-array name="tab_switcher_values">
                  <item>0</item>
                  <item>1</item>
                  <item>2</item>
                  <item>3</item>
                  <item>4</item>
              </string-array>
          </resources>"""

          target_arr = "chrome/android/java/res/values/arrays.xml"
          if os.path.exists(target_arr):
              with open(target_arr, "r", encoding="utf-8") as f:
                  txt = f.read()
              if "tab_switcher_entries" not in txt:
                  txt = txt.replace("</resources>", arrays_data)
                  with open(target_arr, "w", encoding="utf-8") as f:
                      f.write(txt)
          else:
              for sf in glob.glob("chrome/**/res/values/strings.xml", recursive=True):
                  with open(sf, "r", encoding="utf-8") as f:
                      txt = f.read()
                  if "tab_switcher_entries" not in txt:
                      txt = txt.replace("</resources>", arrays_data)
                      with open(sf, "w", encoding="utf-8") as f:
                          sf.write(txt)
                      break

          # 3. Hook Preference in Java
          for root, dirs, files in os.walk("chrome"):
              for f in files:
                  if f.endswith(".java"):
                      jp = os.path.join(root, f)
                      try:
                          with open(jp, "r", encoding="utf-8", errors="ignore") as f:
                              jc = f.read()
                      except Exception:
                          continue
                      if "isGridTabSwitcherEnabled" in jc and "Context context" in jc:
                          hook = """public static boolean isGridTabSwitcherEnabled(Context context) {
                  try {
                      android.content.SharedPreferences sp = android.preference.PreferenceManager.getDefaultSharedPreferences(context);
                      String mode = sp.getString("tab_switcher_type", "1");
                      if ("1".equals(mode) || "4".equals(mode)) return false;
                  } catch (Exception ignored) {}
          """
                          jc = jc.replace("public static boolean isGridTabSwitcherEnabled(Context context) {", hook)
                          with open(jp, "w", encoding="utf-8") as f:
                              f.write(jc)
          '
          find chrome/ -name "*.rej" -delete || true
          find chrome/ -name "*.orig" -delete || true
          find . -name "*.rej" -delete || true
          find . -name "*.orig" -delete || true

          TARGET_FILE="components/browser_ui/site_settings/android/java/src/org/chromium/components/browser_ui/site_settings/SiteSettingsCategory.java"
          if [ -f "$TARGET_FILE" ]; then
            sed -i '/Type\.AUTOPLAY,/d' "$TARGET_FILE"
            sed -i '/Type\.AUTOPLAY/d' "$TARGET_FILE"
          fi

      # 9. DOWNLOAD UBLOCK ORIGIN
      - name: Download uBlock Origin CRX
        run: |
          mkdir -p /home/runner/chromium/src/chrome/browser/resources/bare
          curl -L -f -o /home/runner/chromium/src/chrome/browser/resources/bare/ublock_origin.crx \
            https://github.com/gorhill/uBlock/releases/download/1.74.1b5/uBlock0_1.74.1b5.chromium.crx

      # 10. CONFIGURE GN WITH CCACHE ENABLED
      - name: Configure GN Args (with CCache)
        run: |
          cd /home/runner/chromium/src
          mkdir -p out/Default
          cat << 'EOF' > out/Default/args.gn
          target_os = "android"
          target_cpu = "arm64"
          is_debug = false
          is_component_build = false
          is_official_build = false
          symbol_level = 0
          blink_symbol_level = 0
          v8_symbol_level = 0
          dcheck_always_on = false
          ffmpeg_branding = "Chrome"
          proprietary_codecs = true
          enable_nacl = false
          concurrent_links = 1
          cc_wrapper = "ccache"
          EOF
          /home/runner/depot_tools/gn gen out/Default

      # 11. COMPILE APK (CCACHE ACCELERATED)
      - name: Compile APK
        run: |
          cd /home/runner/chromium/src
          export DEPOT_TOOLS_METRICS=0
          CORES=$(nproc)
          /home/runner/depot_tools/autoninja -C out/Default -j $CORES chrome_public_apk

      # 12. PRINT CCACHE STATS (Shows how many thousands of files were cached)
      - name: Print CCache Statistics
        if: always()
        run: |
          ccache -s

      # 13. PRINT FAILURE LOGS IF FAILED
      - name: Print Failure Logs
        if: failure()
        run: |
          cd /home/runner/chromium/src
          if [ -f out/Default/siso_failed_commands.sh ]; then
            cat out/Default/siso_failed_commands.sh
          fi
          if [ -f out/Default/siso_output ]; then
            tail -n 120 out/Default/siso_output
          fi

      # 14. UPLOAD APK ARTIFACT
      - name: Upload Finished APK
        uses: actions/upload-artifact@v4
        with:
          name: BareBrowser-KiwiSwitcher-ARM64
          path: /home/runner/chromium/src/out/Default/apks/ChromePublic.apk
          if-no-files-found: error