From 7c50864d44afadba845688638094241e3e09e2eb Mon Sep 17 00:00:00 2001
From: Bare <git@barebrowser.org>
Date: Tue, 25 Aug 2026 00:55:40 +0000
Subject: [PATCH] Stop settings search crashing on a row patch 0080 removed

Patch 0080 took the Safe Browsing row out of privacy_preferences.xml but left
the search index still rewriting its summary. updateEntrySummaryForKey throws
when the key is not there, so SettingsActivity died in onCreate with

  IllegalStateException: Existing ID cannot be found:
  ...PrivacySettings#safe_browsing

Two ways in. Long pressing Home and choosing Edit homepage, which is how this
was reported, and tapping Search settings, which is worse and was not
reported. Both open SettingsActivity along a path that builds the index.

I left that call in during 0080 on the reasoning that it would quietly do
nothing for a missing key. It throws instead.

Reproduced and fixed on a Pixel 3: both paths open cleanly, and searching for
safe returns nothing rather than taking the browser down.
---
 .../chrome/browser/privacy/settings/PrivacySettings.java      | 4 ----
 1 file changed, 4 deletions(-)

diff --git a/chrome/android/java/src/org/chromium/chrome/browser/privacy/settings/PrivacySettings.java b/chrome/android/java/src/org/chromium/chrome/browser/privacy/settings/PrivacySettings.java
index 683ae3e7b5..99a966bacc 100644
--- a/chrome/android/java/src/org/chromium/chrome/browser/privacy/settings/PrivacySettings.java
+++ b/chrome/android/java/src/org/chromium/chrome/browser/privacy/settings/PrivacySettings.java
@@ -86,7 +86,6 @@ public class PrivacySettings extends ChromeBaseSettingsFragment
     private static final String PREF_HTTPS_FIRST_MODE_LEGACY = "https_first_mode_legacy";
     private static final String PREF_SECURE_DNS = "secure_dns";
     private static final String PREF_USAGE_STATS = "usage_stats_reporting";
-    private static final String PREF_SAFE_BROWSING = "safe_browsing";
     private static final String PREF_PASSWORD_LEAK_DETECTION = "password_leak_detection";
     private static final String PREF_SYNC_AND_SERVICES_LINK = "sync_and_services_link";
     private static final String PREF_PRIVACY_GUIDE = "privacy_guide";
@@ -558,9 +557,6 @@ public class PrivacySettings extends ChromeBaseSettingsFragment
                         indexData.removeEntry(getUniqueId(PREF_ADVANCED_PROTECTION_INFO));
                     }
 
-                    // The summary in 'Safe Browsing' is a template string. Removes it.
-                    indexData.updateEntrySummaryForKey(
-                            frag, PREF_SAFE_BROWSING, /* summaryId= */ 0);
                 }
             };
 }
