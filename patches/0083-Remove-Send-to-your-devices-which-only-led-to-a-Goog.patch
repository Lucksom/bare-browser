From 2e07469465c7fb0e69db0faf8b9428e0886e54c2 Mon Sep 17 00:00:00 2001
From: Bare <git@barebrowser.org>
Date: Sun, 23 Aug 2026 17:17:01 +0000
Subject: [PATCH] Remove Send to your devices, which only led to a Google
 sign-in

The share submenu offered Send to your devices on every non-incognito tab.
Tapping it opened a Chrome sign-in page, because the feature needs Chrome
Sync to know what the other devices are.

That was not an accident of this build. GetEntryPointDisplayReason returns
kOfferSignIn for a signed-out user, so upstream shows the entry point in
order to recruit a sign-in. Here the recruitment cannot succeed.

The gate now returns nothing for the signed-out and reauth cases, which
removes the entry point from every surface that asks it, including both tab
context menus. The share submenu row is deleted outright: it never consulted
the gate, which is why it appeared regardless.

Share, Copy link and QR code are untouched.
---
 .../chrome/browser/ChromeTabbedActivity.java       |  9 ---------
 .../tabbed_mode/SaveAndShareItemBuilder.java       | 14 --------------
 .../TabbedAppMenuPropertiesDelegate.java           |  6 +++---
 .../send_tab_to_self/entry_point_display_reason.cc | 13 +++++++------
 4 files changed, 10 insertions(+), 32 deletions(-)

diff --git a/chrome/android/java/src/org/chromium/chrome/browser/ChromeTabbedActivity.java b/chrome/android/java/src/org/chromium/chrome/browser/ChromeTabbedActivity.java
index bbad00e9c5..672b28914e 100644
--- a/chrome/android/java/src/org/chromium/chrome/browser/ChromeTabbedActivity.java
+++ b/chrome/android/java/src/org/chromium/chrome/browser/ChromeTabbedActivity.java
@@ -4314,15 +4314,6 @@ public class ChromeTabbedActivity extends ChromeActivity implements PreAttachInt
                             /* notifyOnSuccess= */ true);
             RecordUserAction.record("MobileMenuCopyLink");
             return true;
-        } else if (id == R.id.send_to_devices_menu_id) {
-            assert currentTab != null;
-            ShareDelegate shareDelegate = getShareDelegateSupplier().get();
-            if (shareDelegate != null) {
-                shareDelegate.sendTabToSelf(currentTab);
-                RecordUserAction.record("MobileMenuSendToDevices");
-                return true;
-            }
-            return false;
         } else if (id == R.id.qr_code_menu_id) {
             assert currentTab != null;
             QrCodeCoordinator qrCodeCoordinator =
diff --git a/chrome/android/java/src/org/chromium/chrome/browser/tabbed_mode/SaveAndShareItemBuilder.java b/chrome/android/java/src/org/chromium/chrome/browser/tabbed_mode/SaveAndShareItemBuilder.java
index 0f0247f92e..786837ea24 100644
--- a/chrome/android/java/src/org/chromium/chrome/browser/tabbed_mode/SaveAndShareItemBuilder.java
+++ b/chrome/android/java/src/org/chromium/chrome/browser/tabbed_mode/SaveAndShareItemBuilder.java
@@ -62,20 +62,6 @@ public class SaveAndShareItemBuilder {
                 /* showIcon= */ false);
     }
 
-    /** Builds the "Send to devices" menu item. */
-    public ListItem buildSendToDevicesItem() {
-        assert !isIncognitoShowing();
-        return AppMenuItemUtils.createStandardListItem(
-                AppMenuItemUtils.buildModelForStandardMenuItem(
-                        mContext,
-                        mAppMenuItemTheme,
-                        R.id.send_to_devices_menu_id,
-                        R.string.send_tab_to_self,
-                        Resources.ID_NULL,
-                        mIsMenuIconAtStart),
-                /* showIcon= */ false);
-    }
-
     /** Builds the "QR Code" menu item. */
     public ListItem buildShareQrCodeItem() {
         return AppMenuItemUtils.createStandardListItem(
diff --git a/chrome/android/java/src/org/chromium/chrome/browser/tabbed_mode/TabbedAppMenuPropertiesDelegate.java b/chrome/android/java/src/org/chromium/chrome/browser/tabbed_mode/TabbedAppMenuPropertiesDelegate.java
index c440f15eae..22834e4645 100644
--- a/chrome/android/java/src/org/chromium/chrome/browser/tabbed_mode/TabbedAppMenuPropertiesDelegate.java
+++ b/chrome/android/java/src/org/chromium/chrome/browser/tabbed_mode/TabbedAppMenuPropertiesDelegate.java
@@ -1179,9 +1179,9 @@ public class TabbedAppMenuPropertiesDelegate extends AppMenuPropertiesDelegateIm
         if (ShareUtils.shouldEnableShare(currentTab)) {
             submenuItems.add(buildShareListItem(/* showIcon= */ false));
             submenuItems.add(mSaveAndShareItemBuilder.buildCopyLinkItem());
-            if (currentTab != null && !currentTab.isIncognito()) {
-                submenuItems.add(mSaveAndShareItemBuilder.buildSendToDevicesItem());
-            }
+            // De-Googled build: no "Send to your devices". Unlike the tab context
+            // menus, this row never consulted GetEntryPointDisplayReason, so it
+            // showed for everyone and led to a Google sign-in page.
             submenuItems.add(mSaveAndShareItemBuilder.buildShareQrCodeItem());
         }
 
diff --git a/components/send_tab_to_self/entry_point_display_reason.cc b/components/send_tab_to_self/entry_point_display_reason.cc
index a5be45c2f5..a1a64abe59 100644
--- a/components/send_tab_to_self/entry_point_display_reason.cc
+++ b/components/send_tab_to_self/entry_point_display_reason.cc
@@ -76,12 +76,13 @@ std::optional<EntryPointDisplayReason> GetEntryPointDisplayReason(
     return std::nullopt;
   }
 
-  if (ShouldOfferSignin(sync_service, pref_service)) {
-    return EntryPointDisplayReason::kOfferSignIn;
-  }
-
-  if (ShouldOfferReauth(sync_service, pref_service)) {
-    return EntryPointDisplayReason::kOfferReauth;
+  // De-Googled build: never show the entry point in order to ask for a Google
+  // account. Upstream offers send-tab-to-self to signed-out users precisely to
+  // recruit a sign-in, which in this build lands on a page that cannot complete.
+  // Returning nullopt removes the entry point from every surface that asks here.
+  if (ShouldOfferSignin(sync_service, pref_service) ||
+      ShouldOfferReauth(sync_service, pref_service)) {
+    return std::nullopt;
   }
 
   if (!send_tab_to_self_model->IsReady()) {
