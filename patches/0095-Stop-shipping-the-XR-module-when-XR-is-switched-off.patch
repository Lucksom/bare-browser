From a14787f22d748101281f001b6311402547352556 Mon Sep 17 00:00:00 2001
From: Bare <git@barebrowser.org>
Date: Tue, 25 Aug 2026 18:13:27 +0000
Subject: [PATCH] Stop shipping the XR module when XR is switched off

chrome_module_descs is built without consulting the XR buildflags, so the
xr module was packaged whatever they said. This build has enable_vr,
enable_arcore, enable_openxr and enable_cardboard all off, and the
generated buildflags agree: ENABLE_VR, ENABLE_ARCORE, ENABLE_OPENXR and
ENABLE_CARDBOARD are all 0. The module shipped anyway, carrying the
AndroidX XR and ARCore AARs.

That came to about 20 MB of native code in the APK, none of it reachable:

  libimpress_api_jni.so            18.64 MB
  libandroidx.xr.arcore.openxr.so   0.70 MB
  libandroidx.xr.runtime.openxr.so  0.56 MB
  libarcore_sdk_jni.so              0.10 MB
  libarcore_sdk_c.so                0.07 MB

plus the module's own dex and a handful of immersive video layouts.

The module has exactly two entry points, both in ChromeTabbedActivity and
both behind DeviceInfo.isXr(), so a phone never asks for it. That is also
what this gives up: on an Android XR device the browser would no longer
get a SceneCore session, which is what places a flat browser window in a
spatial environment. This build targets phones, ships arm64 only, and has
the XR code compiled out already, so the module had nothing left to do.

The condition uses the flags upstream already defines rather than a new
one, and matches how the rest of the tree gates this code.
---
 .../modules/chrome_feature_modules.gni        | 20 ++++++++++++++++++-
 1 file changed, 19 insertions(+), 1 deletion(-)

diff --git a/chrome/android/modules/chrome_feature_modules.gni b/chrome/android/modules/chrome_feature_modules.gni
index a34a5fe875..8403bbe4b3 100644
--- a/chrome/android/modules/chrome_feature_modules.gni
+++ b/chrome/android/modules/chrome_feature_modules.gni
@@ -30,8 +30,26 @@ import("//device/vr/buildflags/buildflags.gni")
 chrome_module_descs = [
   on_demand_module_desc,
   stack_unwinder_module_desc,
-  xr_module_desc,
 ]
+
+# Ship the XR module only when something in the build asks for it.
+#
+# The list above is assembled without consulting the XR buildflags, so the
+# module was packaged even with enable_vr, enable_arcore, enable_openxr and
+# enable_cardboard all off. It is not small: the AndroidX XR and ARCore AARs
+# bring libimpress_api_jni.so and four smaller libraries, about 20 MB of native
+# code in the APK, none of which anything can reach once the XR code that would
+# call it has been compiled out.
+#
+# The two entry points into the module, in ChromeTabbedActivity, are both behind
+# DeviceInfo.isXr(), so on a phone the module is never asked for. That also says
+# what this gives up: on an Android XR device the browser would no longer get a
+# SceneCore session, which is how a flat browser window is placed in a spatial
+# environment. This build targets phones and has XR compiled out already, so
+# that is a trade worth making here rather than a loss.
+if (enable_vr || enable_arcore) {
+  chrome_module_descs += [ xr_module_desc ]
+}
 if (dfmify_dev_ui) {
   chrome_module_descs += [ dev_ui_module_desc ]
 }
